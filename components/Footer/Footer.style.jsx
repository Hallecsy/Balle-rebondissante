import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container: {
        backgroundColor: '#000',
        width: '100%',
        height: 70,
        justifyContent: 'center',
        alignItems: 'center',
    },
    footer: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 20,
    }
})